{
  "name": "attendance-app",
  "version": "1.0.0",
  "description": "تطبيق مراقبة الدوام",
  "scripts": {
    "build:css": "npx tailwindcss -c tailwind.config.js -i www/css/style.src.css -o www/css/tailwind.css --minify",
    "copy": "npx cap copy",
    "build": "npm run build:css && npx cap copy"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/local-notifications": "^7.0.1",
    "cordova-android": "^14.0.1",
    "cors": "^2.8.5",
    "firebase-admin": "^13.4.0",
    "google-spreadsheet": "^3.3.0",  
    "dotenv": "^16.0.3",  
    "express": "^4.18.2",

  },
  "devDependencies": {
    "tailwindcss": "^3.0.0"
  },
  "cordova": {
    "platforms": [
      "android"
    ]
  }
}
