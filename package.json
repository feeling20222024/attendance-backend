{
  "name": "attendance-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node decrypt-env.js && node server.js",
    "build:css": "npx tailwindcss -c tailwind.config.js -i www/css/style.src.css -o www/css/tailwind.css --minify",
    "copy": "npx cap copy",
    "build": "npm run build:css && npx cap copy"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "google-spreadsheet": "^3.3.0",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.0"
  }
}
