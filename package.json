{
  "name": "attendance-backend",
  "version": "1.0.0",
  "description": "Backend for AttendanceApp (Express + Google Sheets)",
  "main": "server.js",
  "scripts": {
    "start": "node --openssl-legacy-provider server.js",
    "dev": "nodemon --watch \"./**/*.js\" --exec \"node --openssl-legacy-provider server.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "google-spreadsheet": "^3.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
